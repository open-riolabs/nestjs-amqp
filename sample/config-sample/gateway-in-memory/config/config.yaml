app:
  port: 3000
  host: 0.0.0.0
  environment: development

# This example keeps ACL/gateway-admin data fully in RAM (in-memory repositories) and the
# ACL L2 cache in RAM too — no Mongo/Redis needed. Only RabbitMQ is required.

broker:
  name: "rabbitmq"
  uri: "amqp://localhost:5672/"
  defaultSubscribeErrorBehavior: "ack"
  defaultPublishErrorBehavior: "reject"
  # Route auto-discovery (publisher side) lives INSIDE the broker block. This app also plays the
  # demo microservice and announces its decorator-discovered routes on boot. serviceName would also
  # fill connection_name if it weren't already set above (here connection_name wins).
  # exchange/queue default to rlb-route-discovery / rlb-route-sync (override to namespace per env).
  routeDiscovery:
    serviceName: demo-ms
    publishOnBoot: true
  connectionManagerOptions:
    heartbeatIntervalInSeconds: 60
    reconnectTimeInSeconds: 60
    connectionOptions:
      clientProperties:
        connection_name: "gateway-in-memory"
      credentials:
        mechanism: PLAIN
        username: REDACTED
        password: REDACTED

  exchanges:
    - name: rlb
      type: direct
      createExchangeIfNotExists: true
      options:
        durable: true

  queues:
    # Queue consumed by the ACL backend handlers (AclService / AclManagementService).
    - name: rlb-acl
      exchange: rlb
      routingKey: rlb-acl
      createQueueIfNotExists: true
      options:
        durable: true
    # Queue consumed by the gateway-admin backend handlers.
    - name: rlb-gateway-admin
      exchange: rlb
      routingKey: rlb-gateway-admin
      createQueueIfNotExists: true
      options:
        durable: true
    # Queue for the route-discovery demo microservice (RouteDiscoveryDemoService).
    - name: demo-ms
      exchange: rlb
      routingKey: demo-ms
      createQueueIfNotExists: true
      options:
        durable: true

  # replyQueues:
  #   rlb: rlb-reply

topics:
  # Topic the @BrokerAction handlers of the ACL module bind to (ACL_TOPIC = 'rlb-acl').
  - name: rlb-acl
    mode: rpc
    queue: rlb-acl
    exchange: rlb
    routingKey: rlb-acl
  # Topic the gateway-admin handlers bind to (GATEWAY_ADMIN_TOPIC = 'rlb-gateway-admin').
  - name: rlb-gateway-admin
    mode: rpc
    queue: rlb-gateway-admin
    exchange: rlb
    routingKey: rlb-gateway-admin
  # Broadcast control topic: the gateway subscribes (per-instance queue) and rebuilds its
  # Express routes at runtime when a message arrives. A direct exchange + same routingKey
  # fans out to every instance's queue (each instance needs a distinct connection_name).
  - name: rlb-gateway-control
    mode: broadcast
    exchange: rlb
    routingKey: rlb-gateway-control
  # Topic the route-discovery demo microservice binds its @BrokerAction handlers to.
  - name: demo-ms
    mode: rpc
    queue: demo-ms
    exchange: rlb
    routingKey: demo-ms

auth-providers:
  - name: gateway-jwks
    type: jwks
    jwtMap:
      - sub:userId
      - email:email
      - preferred_username:username
      - roles:roles
    rolesClaim: roles
    headerPrefix: "X-GTW-AUTH-"
    uidClaim: USERID
    usernameClaim: USERNAME
    outProp: $auth
    issuer: https://auth.example.com/realms/demo
    tokenUrl: https://auth.example.com/realms/demo/protocol/openid-connect/token
    jwksUri: https://auth.example.com/realms/demo/protocol/openid-connect/certs
    algorithms:
      - RS256
    clientId: demo-client
    #clientSecret:
    #audience:
    scopes:
      - openid
      - profile

gateway:
  mode: gateway
  paths:
    # --- health ---
    - name: health
      method: GET
      path: /health
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-health
      mode: rpc

    # --- ACL management: actions (name is the key — PUT upserts, GET lists, DELETE by name) ---
    - name: acl-action-list
      method: GET
      path: /acl/actions
      dataSource: query
      topic: rlb-acl
      action: acl-action-list
      mode: rpc
    - name: acl-action-get        # query: ?name=
      method: GET
      path: /acl/actions/get
      dataSource: query
      topic: rlb-acl
      action: acl-action-get
      mode: rpc
    - name: acl-action-upsert     # PUT creates-or-updates by name. body: { name, description? }
      method: PUT
      path: /acl/actions
      dataSource: body
      topic: rlb-acl
      action: acl-action-update
      mode: rpc
    - name: acl-action-delete     # body: { name }
      method: DELETE
      path: /acl/actions
      dataSource: body
      topic: rlb-acl
      action: acl-action-delete
      mode: rpc
    - name: acl-action-search     # query: ?q=&limit=
      method: GET
      path: /acl/actions/search
      dataSource: query
      topic: rlb-acl
      action: acl-action-search
      mode: rpc

    # --- ACL management: roles (name is the key — PUT upserts, GET lists/gets, DELETE by name) ---
    - name: acl-role-list
      method: GET
      path: /acl/roles
      dataSource: query
      topic: rlb-acl
      action: acl-role-list
      mode: rpc
    - name: acl-role-get          # query: ?name=
      method: GET
      path: /acl/roles/get
      dataSource: query
      topic: rlb-acl
      action: acl-role-get
      mode: rpc
    - name: acl-role-upsert       # PUT creates-or-updates by name. body: { name, actions, description? }
      method: PUT
      path: /acl/roles
      dataSource: body
      topic: rlb-acl
      action: acl-role-update
      mode: rpc
    - name: acl-role-delete       # body: { name }
      method: DELETE
      path: /acl/roles
      dataSource: body
      topic: rlb-acl
      action: acl-role-delete
      mode: rpc
    - name: acl-role-search       # query: ?q=&limit=
      method: GET
      path: /acl/roles/search
      dataSource: query
      topic: rlb-acl
      action: acl-role-search
      mode: rpc

    # --- ACL grants (per-user; resourceId optional) ---
    - name: acl-grant
      method: POST
      path: /acl/grants
      dataSource: body
      topic: rlb-acl
      action: acl-grant
      mode: rpc
    - name: acl-revoke
      method: DELETE
      path: /acl/grants
      dataSource: body
      topic: rlb-acl
      action: acl-revoke
      mode: rpc
    - name: acl-grant-search      # query: ?q=&limit=
      method: GET
      path: /acl/grants/search
      dataSource: query
      topic: rlb-acl
      action: acl-grant-search
      mode: rpc

    # --- ACL check (GET → 200 with true/false) ---
    # checkAction(userId, {companyId?, resourceId?}, action): true if the user holds `action`
    # via any role on the EXACT (companyId, resourceId). Both ids absent ⇒ resource-less grants
    # only (no wildcard). query: ?userId=&action=role-management&companyId=&resourceId=
    - name: acl-check-action
      method: GET
      path: /acl/check
      dataSource: query
      topic: rlb-acl
      action: acl-check-action
      mode: rpc
    # Authenticated: lists the caller's accessible resources (grouped by business resource).
    # Reads userId from the forwarded X-GTW-AUTH-USERID header → needs `auth` (no actions).
    - name: acl-list-resources-by-user
      method: GET
      path: /acl/resources
      dataSource: query
      topic: rlb-acl
      action: acl-list-resources-by-user
      mode: rpc
      auth: gateway-jwks

    # --- gateway-admin: manage DB routes / auth-providers / metrics ---
    - name: gw-path-create
      method: POST
      path: /admin/paths
      dataSource: body
      topic: rlb-gateway-admin
      action: gw-path-create
      mode: rpc
    - name: gw-path-list
      method: GET
      path: /admin/paths
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-path-list
      mode: rpc
    - name: gw-path-search        # query: ?q=&limit=
      method: GET
      path: /admin/paths/search
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-path-search
      mode: rpc
    - name: gw-path-export
      method: GET
      path: /admin/paths/export
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-path-export
      mode: rpc
    - name: gw-path-update        # body: { id, name, method, path, topic, action, mode, ... }
      method: PUT
      path: /admin/paths
      dataSource: body
      topic: rlb-gateway-admin
      action: gw-path-update
      mode: rpc
    - name: gw-path-get           # query: ?id=
      method: GET
      path: /admin/paths/get
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-path-get
      mode: rpc
    - name: gw-path-delete
      method: DELETE
      path: /admin/paths
      dataSource: body
      topic: rlb-gateway-admin
      action: gw-path-delete
      mode: rpc
    # Route-change journal (who changed what): actor=system for auto-discovery, userId for manual CRUD.
    - name: gw-route-log-list
      method: GET
      path: /admin/route-log
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-route-log-list
      mode: rpc
    - name: gw-route-log-search # ?actor=&service=&event=&routeKey=&from=&to=&page=&limit=
      method: GET
      path: /admin/route-log/search
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-route-log-search
      mode: rpc
    - name: gw-auth-list
      method: GET
      path: /admin/auth
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-auth-list
      mode: rpc
    - name: gw-auth-search        # query: ?q=&limit=
      method: GET
      path: /admin/auth/search
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-auth-search
      mode: rpc
    - name: gw-auth-upsert        # PUT creates-or-updates by name. body: { name, type, ... }
      method: PUT
      path: /admin/auth
      dataSource: body
      topic: rlb-gateway-admin
      action: gw-auth-update
      mode: rpc
    - name: gw-auth-get           # query: ?name=
      method: GET
      path: /admin/auth/get
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-auth-get
      mode: rpc
    - name: gw-auth-delete
      method: DELETE
      path: /admin/auth
      dataSource: body
      topic: rlb-gateway-admin
      action: gw-auth-delete
      mode: rpc
    - name: gw-metrics-get
      method: GET
      path: /admin/metrics
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-get
      mode: rpc
    - name: gw-metrics-series
      method: GET
      path: /admin/metrics/series
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-series
      mode: rpc
    - name: gw-metrics-points
      method: GET
      path: /admin/metrics/points
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-points
      mode: rpc
    - name: gw-metrics-summary
      method: GET
      path: /admin/metrics/summary
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-summary
      mode: rpc
    - name: gw-metrics-prometheus
      method: GET
      path: /admin/metrics/prometheus
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-prometheus
      mode: rpc
      headers:
        Content-Type: text/plain; version=0.0.4
    - name: gw-metrics-rollups
      method: GET
      path: /admin/metrics/rollups
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-rollups
      mode: rpc
    - name: gw-metrics-track
      method: POST
      path: /admin/metrics/track
      dataSource: body
      topic: rlb-gateway-admin
      action: gw-metrics-track
      mode: event
    - name: gw-reload
      method: POST
      path: /admin/reload
      dataSource: body
      topic: rlb-gateway-control
      action: gw-reload
      mode: event
    - name: gw-auth-reload # deliberate: reload DB auth-providers into RAM (separate from gw-reload)
      method: POST
      path: /admin/auth/reload
      dataSource: body
      topic: rlb-gateway-control
      action: gw-auth-reload
      mode: event

    # --- PROTECTED demo: Keycloak JWT + actions → gateway action check (checkAction) ---
    # `actions` lists ACTION names now (NOT roles): the gateway resolves the caller's userId
    # from the token, extracts the request's (companyId, resourceId), and passes if the user
    # holds AT LEAST ONE of these actions on that (companyId, resourceId). With no companyId/
    # resourceId on the request, a resource-less grant of one of these actions is required.
    - name: protected-resource
      method: GET
      path: /protected
      dataSource: query
      topic: rlb-gateway-admin
      action: gw-metrics-get
      mode: rpc
      auth: gateway-jwks
      actions: [gateway-access]

  # Routes managed in the DB are loaded (ordered static-before-param) at boot AND on every
  # runtime reload, in addition to the YAML ones above. Seed via POST /admin/paths then
  # POST /admin/reload (no restart needed).
  loadConfig:
    paths:
      topic: rlb-gateway-admin
      action: gw-path-export
  # Broadcast topic that triggers a runtime route rebuild (see topics: rlb-gateway-control).
  reloadTopic: rlb-gateway-control
  # Per-call metrics: the gateway auto-emits a track event after every request to the
  # gateway-admin metrics handler (GatewayMetricsService.track). No manual call needed.
  metrics:
    topic: rlb-gateway-admin
    action: gw-metrics-track
  events: []
